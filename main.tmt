~ function tests

func add(int x, int y) yields int {
    int add = x + y
    harvest add
}

func double(int x) yields int {
    x = x * 2
    harvest x
}

sprout {
    ~ call nested function
    print("the answer is:")
    print(double(add(1, 2)))

    ~ end program.
    harvest tomato
}